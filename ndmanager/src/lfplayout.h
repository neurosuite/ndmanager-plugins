#ifndef LFPLAYOUT_H
#define LFPLAYOUT_H

#include "ui_lfplayout.h"


class LfpLayout : public QWidget, private Ui_LfpLayout
{
    Q_OBJECT

public:
    LfpLayout( QWidget* parent = 0 );
    

public slots:
   // TODO copy 0 functions, 0 signals, and 0 slots (not init and destroy!) from lfplayout.h.old here

private:
   
};

#endif
