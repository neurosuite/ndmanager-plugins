#ifndef LAUNCHERLAYOUT_H
#define LAUNCHERLAYOUT_H

#include "ui_launcherlayout.h"


class LauncherLayout : public QWidget, private Ui_LauncherLayout
{
    Q_OBJECT

public:
    LauncherLayout( QWidget* parent = 0 );
    

public slots:
   // TODO copy 0 functions, 0 signals, and 0 slots (not init and destroy!) from launcherlayout.h.old here

private:
   
};

#endif
