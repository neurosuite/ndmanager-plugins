#ifndef FILELAYOUT_H
#define FILELAYOUT_H

#include "ui_filelayout.h"


class FileLayout : public QWidget, private Ui_FileLayout
{
    Q_OBJECT

public:
    FileLayout( QWidget* parent = 0 );
    

public slots:
   // TODO copy 0 functions, 0 signals, and 0 slots (not init and destroy!) from filelayout.h.old here

private:
   
};

#endif
